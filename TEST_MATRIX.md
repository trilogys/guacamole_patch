# 端到端验收矩阵

每项至少在 Chrome 和 Edge 各执行一次。测试前远程 Windows 打开记事本。

## A. 远程微软拼音

配置：Guacamole 输入方式“无”，远程微软拼音，RDP 布局 `en-us-qwerty`。

| 编号 | 操作 | 期望 |
| --- | --- | --- |
| A1 | 输入 `zhongwen` 并选词 | 远程出现中文 |
| A2 | 使用数字键、空格、Enter 选词 | 候选操作正常 |
| A3 | 使用 Backspace、Delete、左右方向键 | 远程输入法和文本正常响应 |
| A4 | 普通切出/切回 20 次 | 无需刷新，继续输入 |
| A5 | 按住 Ctrl 后切出，在其他标签页松开，再切回 | 远端 Ctrl 不保持按下 |
| A6 | 分别对 Shift、Alt、Windows 键重复 A5 | 远端无卡键 |
| A7 | 切回时立即连续输入 | 首个字符不丢失或异常 |
| A8 | 打开 Guacamole 菜单再切换标签页 | 菜单表单焦点不被抢走 |
| A9 | 切到其他标签页或应用 10 分钟以上，再切回并点击远程画面 | 无需刷新，首次点击后可继续使用微软拼音 |
| A10 | 长时间切出后直接按 `Ctrl+Alt+Shift` | 自动恢复输入捕获并切换 Guacamole 侧边菜单 |
| A11 | 生命周期事件遗漏时按 `Ctrl+Alt+K` | 无需刷新即可恢复键盘和输入法 |
| A12 | 本机切到中文输入法，远程切到 ENG 后输入字母 | 输入英文，不出现本机中文候选或中文文本 |
| A13 | 本机保持中文输入法，远程切到微软拼音后输入拼音 | 由远程微软拼音显示候选并输入中文 |

## B. Guacamole 文本输入

配置：远程 ENG，Guacamole“文本输入”，本机中文输入法。

| 编号 | 操作 | 期望 |
| --- | --- | --- |
| B1 | 输入并确认中文 | 中文进入远程记事本 |
| B2 | 切出时保留未确认拼音，切回后重新输入 | 不永久卡死；未确认内容允许被丢弃 |
| B3 | 切回后输入中文 | 左下角记录短暂出现，远程同步出现 |
| B4 | Backspace/Delete | 删除远程文本，而非要求删除左下角发送记录 |
| B5 | 切换 20 次 | 不需要刷新 |
| B6 | 切回时观察输入焦点 | 可见文本输入框保持或自动恢复焦点，隐藏 InputSink 不抢焦点 |
| B7 | 切到其他标签页或应用 10 分钟以上，再切回并点击远程画面 | 无需刷新，首次点击后本机中文输入法恢复 |

## C. 网络提示

| 编号 | 操作 | 期望 |
| --- | --- | --- |
| C1 | 使用浏览器网络限速制造不足 3 秒的短暂断流 | 不闪现“网络连接似乎不太稳定”提示，连接自行恢复 |
| C2 | 页面可见时持续阻断 Guacamole 隧道数据超过约 4.5 秒 | 显示网络不稳定提示；数据恢复后提示消失 |
| C3 | 切到其他标签页后等待 10 秒再返回，连接仍正常 | 不在返回瞬间显示网络不稳定提示 |
| C4 | 标签页隐藏期间开始持续断流，返回后继续阻断 3 秒 | 返回后的确认窗口结束后显示提示 |
| C5 | 持续阻断超过底层接收超时 | 连接仍按 Guacamole 原有逻辑报错并断开，不会无限等待 |
| C6 | 网络提示出现后恢复流量并保持稳定 | 5 秒后仅当前异常连接自动重建，Guacamole 登录态和页面保留 |
| C7 | 第一次自动重连后短期内再次制造并恢复异常 | 第二次等待 10 秒后重连；连续第三次不再自动循环，仍可手动重连 |
| C8 | 自动重连等待期间开始文件传输 | 待执行重连取消；传输结束后才重新安排恢复 |
| C9 | 自动重连等待期间点击“保留当前连接” | 自动重连取消，当前远程连接不中断 |
| C10 | 连续稳定运行一分钟后再次制造并恢复异常 | 自动尝试次数已重置，可重新执行一次自动恢复 |
| C11 | 快速连续移动鼠标并单击 | 远程指针跟随最新位置，单击不排在大量旧移动之后 |
| C12 | 按住左键快速拖拽后松开 | 拖拽终点正确，远端不会保持左键按下 |
| C13 | 连续双击、右键和滚轮 | 按钮与滚轮状态均立即响应，不被移动合并丢弃 |
| C14 | 弱网期间高速晃动鼠标后点击固定位置 | 点击优先于后续过期移动，恢复后无长时间控制延迟 |
| C15 | 使用触摸板相对模式拖动和右键 | 模拟鼠标路径同样合并移动并保留按钮状态 |
| C16 | 有待发送移动时触发自动重连 | 旧连接的坐标不会发送到新连接 |
| C17 | 页面可见时持续阻断隧道且不恢复 | 确认异常后自动重建直连会话，无需等待刷新页面 |
| C18 | 隧道保持正常但连续点击三次后 8 秒内没有及时的远端画面同步 | 无同步或相对延迟超过 3 秒时触发连接级恢复，直连会话自动重建 |
| C19 | 在负载均衡连接组中制造相同故障 | 仅显示手动重连，不自动切换到另一个后端画面 |
| C20 | 远程 Chrome/Edge 打开复杂网页并连续滚动 | 正常及时同步时不误重连；画面队列持续积压且点击无及时反馈时触发恢复 |

## D. 回归与安全

| 编号 | 操作 | 期望 |
| --- | --- | --- |
| D1 | Guacamole 登录页输入账号密码并切换标签页 | 密码框焦点不被隐藏输入框抢走 |
| D2 | 连接设置页面编辑字段 | 表单输入正常 |
| D3 | 文件上传、剪贴板、菜单 | 原功能正常 |
| D4 | 断开并重新连接 | 输入法和网络提示修复仍有效 |
| D5 | 回滚官方镜像 | 服务恢复，数据库和连接配置保留 |
| D6 | 查看浏览器控制台 | 无新增 ReferenceError、TypeError 或 Angular digest 错误 |

## E. 可选场景

```text
Firefox
全屏模式
多连接平铺和多选
Android Chrome/WebView
iframe 嵌入
本地 macOS/Linux + 远程 Windows 微软拼音
```

任一必做项目失败时，不应将 `1.6.0-recovery3` 作为正式生产版本。
