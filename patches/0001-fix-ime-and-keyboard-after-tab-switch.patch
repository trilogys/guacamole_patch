diff --git a/guacamole/src/main/frontend/src/app/client/controllers/clientController.js b/guacamole/src/main/frontend/src/app/client/controllers/clientController.js
--- a/guacamole/src/main/frontend/src/app/client/controllers/clientController.js
+++ b/guacamole/src/main/frontend/src/app/client/controllers/clientController.js
@@ -34,6 +34,7 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
 
     // Required services
     const $location              = $injector.get('$location');
+    const $timeout               = $injector.get('$timeout');
     const authenticationService  = $injector.get('authenticationService');
     const connectionGroupService = $injector.get('connectionGroupService');
     const clipboardService       = $injector.get('clipboardService');
@@ -94,6 +95,36 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
      */
     var DEL_KEY = 0xFFFF;
 
+    /**
+     * Initial delay before automatically rebuilding a recovered connection.
+     * Each consecutive attempt doubles this delay.
+     *
+     * @type Number
+     */
+    var AUTO_RECONNECT_DELAY = 5000;
+
+    /**
+     * Maximum consecutive automatic reconnect attempts per connection.
+     *
+     * @type Number
+     */
+    var AUTO_RECONNECT_MAX_ATTEMPTS = 2;
+
+    /**
+     * Stable time after which the automatic reconnect attempt count resets.
+     *
+     * @type Number
+     */
+    var AUTO_RECONNECT_RESET_DELAY = 60000;
+
+    /**
+     * Automatic reconnect attempt counts, pending timers, and reset timers by
+     * ManagedClient ID.
+     */
+    var autoReconnectAttempts = {};
+    var autoReconnectTimers = {};
+    var autoReconnectResetTimers = {};
+
     /**
      * Menu-specific properties.
      */
@@ -144,6 +175,21 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
         $scope.menu.shown = false;
     };
 
+    /**
+     * Requests immediate recovery of Guacamole keyboard and IME capture.
+     */
+    $scope.recoverKeyboardInput = function recoverKeyboardInput() {
+        $scope.$emit('guacForceInputRecovery');
+        $scope.closeMenu();
+    };
+
+    /**
+     * Toggles the Guacamole menu when requested by the global keyboard fallback.
+     */
+    $scope.$on('guacToggleMenuRequested', function toggleMenuRequested() {
+        $scope.menu.shown = !$scope.menu.shown;
+    });
+
     /**
      * Applies any changes to connection parameters made by the user within the
      * Guacamole menu to the given ManagedClient. If no client is supplied,
@@ -661,6 +707,190 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
         return _.findIndex($scope.clientGroup.clients, client => client.clientState.tunnelUnstable) !== -1;
     };
 
+    /**
+     * Returns whether the given client still has an active connection.
+     */
+    var isClientConnectionActive = function isClientConnectionActive(client) {
+        const state = client.clientState.connectionState;
+        return state === ManagedClientState.ConnectionState.WAITING
+            || state === ManagedClientState.ConnectionState.CONNECTED;
+    };
+
+    /**
+     * Returns the currently-attached client having the given ID, if any.
+     */
+    var getAttachedClient = function getAttachedClient(id) {
+        if (!$scope.clientGroup)
+            return null;
+        return _.find($scope.clientGroup.clients, client => client.id === id);
+    };
+
+    /**
+     * Cancels a pending automatic reconnect for the given client ID.
+     */
+    var cancelAutoReconnect = function cancelAutoReconnect(id) {
+        if (autoReconnectTimers[id]) {
+            $timeout.cancel(autoReconnectTimers[id]);
+            delete autoReconnectTimers[id];
+        }
+
+        const client = getAttachedClient(id);
+        if (client)
+            client.autoReconnectPending = false;
+    };
+
+    /**
+     * Cancels a pending stable-period reset for the given client ID.
+     */
+    var cancelAutoReconnectReset = function cancelAutoReconnectReset(id) {
+        if (autoReconnectResetTimers[id]) {
+            $timeout.cancel(autoReconnectResetTimers[id]);
+            delete autoReconnectResetTimers[id];
+        }
+    };
+
+    /**
+     * Schedules the attempt counter to reset after a successful stable period.
+     */
+    var scheduleAutoReconnectReset = function scheduleAutoReconnectReset(id) {
+        cancelAutoReconnectReset(id);
+
+        autoReconnectResetTimers[id] = $timeout(function resetAutoReconnectAttempts() {
+            delete autoReconnectResetTimers[id];
+
+            const client = getAttachedClient(id);
+            if (client && isClientConnectionActive(client)
+                    && !client.clientState.tunnelUnstable) {
+                autoReconnectAttempts[id] = 0;
+            }
+        }, AUTO_RECONNECT_RESET_DELAY);
+    };
+
+    /**
+     * Returns whether the given client can be automatically reconnected now.
+     */
+    var canAutoReconnect = function canAutoReconnect(client) {
+        return client.reconnectSuggested
+            && !client.clientState.tunnelUnstable
+            && isClientConnectionActive(client)
+            && !ManagedClient.hasActiveTransfers(client);
+    };
+
+    /**
+     * Queues a bounded automatic reconnect for the given recovered client.
+     */
+    var queueAutoReconnect = function queueAutoReconnect(client) {
+        const id = client.id;
+        const attempts = autoReconnectAttempts[id] || 0;
+        if (autoReconnectTimers[id] || attempts >= AUTO_RECONNECT_MAX_ATTEMPTS)
+            return;
+
+        const delay = AUTO_RECONNECT_DELAY * Math.pow(2, attempts);
+        client.autoReconnectPending = true;
+
+        autoReconnectTimers[id] = $timeout(function autoReconnectClient() {
+            delete autoReconnectTimers[id];
+            client.autoReconnectPending = false;
+
+            // The client may have been replaced, become unstable again, or
+            // started a transfer while this reconnect was pending.
+            const currentClient = getAttachedClient(id);
+            if (currentClient !== client || !canAutoReconnect(currentClient))
+                return;
+
+            autoReconnectAttempts[id] = attempts + 1;
+            guacClientManager.replaceManagedClient(id);
+        }, delay);
+    };
+
+    /**
+     * Returns whether reconnecting one or more clients should be offered after
+     * a confirmed period of tunnel instability.
+     *
+     * @returns {Boolean}
+     *     true if at least one client may benefit from being reconnected.
+     */
+    $scope.isConnectionRecoveryAvailable = function isConnectionRecoveryAvailable() {
+        return _.findIndex($scope.clientGroup.clients, client =>
+            client.reconnectSuggested && isClientConnectionActive(client)) !== -1;
+    };
+
+    /**
+     * Returns whether an automatic reconnect is currently pending.
+     */
+    $scope.isAutomaticReconnectPending = function isAutomaticReconnectPending() {
+        return _.findIndex($scope.clientGroup.clients,
+            client => client.autoReconnectPending) !== -1;
+    };
+
+    /**
+     * Replaces all clients which experienced confirmed tunnel instability.
+     */
+    $scope.reconnectDegradedClients = function reconnectDegradedClients() {
+        $scope.clientGroup.clients.slice().forEach(client => {
+            if (client.reconnectSuggested && isClientConnectionActive(client)) {
+                cancelAutoReconnect(client.id);
+                cancelAutoReconnectReset(client.id);
+                autoReconnectAttempts[client.id] = 0;
+                guacClientManager.replaceManagedClient(client.id);
+            }
+        });
+    };
+
+    /**
+     * Dismisses the recovery suggestion without reconnecting.
+     */
+    $scope.dismissConnectionRecovery = function dismissConnectionRecovery() {
+        $scope.clientGroup.clients.forEach(client => {
+            cancelAutoReconnect(client.id);
+            client.reconnectSuggested = false;
+        });
+    };
+
+    // Queue or cancel automatic recovery as tunnel and transfer state changes.
+    $scope.$watchCollection(function getAutoReconnectState() {
+        if (!$scope.clientGroup)
+            return [];
+
+        return $scope.clientGroup.clients.map(client => [
+            client.id,
+            client.reconnectSuggested,
+            client.clientState.tunnelUnstable,
+            client.clientState.connectionState,
+            ManagedClient.hasActiveTransfers(client)
+        ].join(':'));
+    }, function autoReconnectStateChanged() {
+
+        if (!$scope.clientGroup) {
+            Object.keys(autoReconnectTimers).forEach(cancelAutoReconnect);
+            Object.keys(autoReconnectResetTimers).forEach(cancelAutoReconnectReset);
+            return;
+        }
+
+        const candidates = {};
+        $scope.clientGroup.clients.forEach(client => {
+            if (canAutoReconnect(client)) {
+                candidates[client.id] = true;
+                queueAutoReconnect(client);
+            }
+
+            const attempts = autoReconnectAttempts[client.id] || 0;
+            if (attempts && isClientConnectionActive(client)
+                    && !client.clientState.tunnelUnstable) {
+                if (!autoReconnectResetTimers[client.id])
+                    scheduleAutoReconnectReset(client.id);
+            }
+            else
+                cancelAutoReconnectReset(client.id);
+        });
+
+        Object.keys(autoReconnectTimers).forEach(id => {
+            if (!candidates[id])
+                cancelAutoReconnect(id);
+        });
+
+    });
+
     /**
      * Immediately disconnects all currently-focused clients, if any.
      */
@@ -870,6 +1100,10 @@ angular.module('client').controller('clientController', ['$scope', '$routeParams
 
     // Clean up when view destroyed
     $scope.$on('$destroy', function clientViewDestroyed() {
+
+        Object.keys(autoReconnectTimers).forEach(cancelAutoReconnect);
+        Object.keys(autoReconnectResetTimers).forEach(cancelAutoReconnectReset);
+
         setAttachedGroup(null);
 
         // always unset fullscreen mode to not confuse user 
diff --git a/guacamole/src/main/frontend/src/app/client/directives/guacTiledClients.js b/guacamole/src/main/frontend/src/app/client/directives/guacTiledClients.js
--- a/guacamole/src/main/frontend/src/app/client/directives/guacTiledClients.js
+++ b/guacamole/src/main/frontend/src/app/client/directives/guacTiledClients.js
@@ -162,6 +162,22 @@ angular.module('client').directive('guacTiledClients', [function guacTiledClient
             };
         };
 
+        // Downstream modification: restore client keyboard focus after tab changes.
+
+        /**
+         * Ensures at least one client in the currently attached group can receive
+         * keyboard events after browser focus or visibility changes.
+         */
+        var restoreClientKeyboardFocus = function restoreClientKeyboardFocus() {
+            if ($scope.clientGroup)
+                ManagedClientGroup.verifyFocus($scope.clientGroup);
+        };
+
+        // Text-input mode and raw-keyboard mode use different browser input
+        // paths, but both require a focused ManagedClient before sending keys.
+        $scope.$on('guacTextInputFocusRequested', restoreClientKeyboardFocus);
+        $scope.$on('guacKeyboardFocusRequested', restoreClientKeyboardFocus);
+
         /**
          * @borrows ManagedClientGroup.hasMultipleClients
          */
diff --git a/guacamole/src/main/frontend/src/app/client/styles/connection-warning.css b/guacamole/src/main/frontend/src/app/client/styles/connection-warning.css
--- a/guacamole/src/main/frontend/src/app/client/styles/connection-warning.css
+++ b/guacamole/src/main/frontend/src/app/client/styles/connection-warning.css
@@ -54,3 +54,15 @@
     background-repeat: no-repeat;
 
 }
+
+#connection-warning .actions {
+    display: flex;
+    flex-wrap: wrap;
+    justify-content: flex-end;
+    gap: 0.5em;
+    margin-top: 0.5em;
+}
+
+#connection-warning .actions button {
+    font-size: 1em;
+}
diff --git a/guacamole/src/main/frontend/src/app/client/templates/client.html b/guacamole/src/main/frontend/src/app/client/templates/client.html
--- a/guacamole/src/main/frontend/src/app/client/templates/client.html
+++ b/guacamole/src/main/frontend/src/app/client/templates/client.html
@@ -41,8 +41,27 @@
     </div>
 
     <!-- Connection stability warning -->
-    <div id="connection-warning" ng-show="isConnectionUnstable()">
-        {{'CLIENT.TEXT_CLIENT_STATUS_UNSTABLE' | translate}}
+    <div id="connection-warning"
+         ng-show="isConnectionUnstable() || isConnectionRecoveryAvailable()">
+        <div class="message" ng-if="isConnectionUnstable()">
+            {{'CLIENT.TEXT_CLIENT_STATUS_UNSTABLE' | translate}}
+        </div>
+        <div class="message"
+             ng-if="!isConnectionUnstable() && isAutomaticReconnectPending()">
+            {{'CLIENT.TEXT_CLIENT_STATUS_RECOVERED_RECONNECTING' | translate}}
+        </div>
+        <div class="message"
+             ng-if="!isConnectionUnstable() && !isAutomaticReconnectPending()">
+            {{'CLIENT.TEXT_CLIENT_STATUS_RECOVERED_SLOW' | translate}}
+        </div>
+        <div class="actions">
+            <button class="reconnect" ng-click="reconnectDegradedClients()">
+                {{'CLIENT.ACTION_RECONNECT' | translate}}
+            </button>
+            <button ng-if="!isConnectionUnstable()" ng-click="dismissConnectionRecovery()">
+                {{'CLIENT.ACTION_DISMISS_RECOVERY' | translate}}
+            </button>
+        </div>
     </div>
     
     <!-- Menu -->
@@ -133,6 +152,12 @@
                     <h3>{{'CLIENT.SECTION_HEADER_INPUT_METHOD' | translate}}</h3>
                     <div class="content">
 
+                        <!-- Manual keyboard/IME recovery -->
+                        <div class="choice">
+                            <button class="button" ng-click="recoverKeyboardInput()">{{'CLIENT.ACTION_RECOVER_KEYBOARD' | translate}}</button>
+                            <p class="caption">{{'CLIENT.HELP_RECOVER_KEYBOARD' | translate}}</p>
+                        </div>
+
                         <!-- No IME -->
                         <div class="choice">
                             <label><input id="ime-none" name="input-method" ng-change="closeMenu()" ng-model="menu.inputMethod" type="radio" value="none"> {{'CLIENT.NAME_INPUT_METHOD_NONE' | translate}}</label>
diff --git a/guacamole/src/main/frontend/src/app/client/types/ManagedClient.js b/guacamole/src/main/frontend/src/app/client/types/ManagedClient.js
--- a/guacamole/src/main/frontend/src/app/client/types/ManagedClient.js
+++ b/guacamole/src/main/frontend/src/app/client/types/ManagedClient.js
@@ -35,6 +35,7 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
     const ManagedDisplay         = $injector.get('ManagedDisplay');
     const ManagedFilesystem      = $injector.get('ManagedFilesystem');
     const ManagedFileUpload      = $injector.get('ManagedFileUpload');
+    const ManagedFileTransferState = $injector.get('ManagedFileTransferState');
     const ManagedShareLink       = $injector.get('ManagedShareLink');
 
     // Required services
@@ -62,6 +63,15 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
      */
     var THUMBNAIL_UPDATE_FREQUENCY = 5000;
 
+    /**
+     * Additional time that tunnel instability must persist before the user is
+     * warned, in milliseconds. The underlying tunnel timeout remains
+     * unchanged and continues to detect actual connection failure.
+     *
+     * @type Number
+     */
+    var TUNNEL_UNSTABLE_WARNING_DELAY = 3000;
+
     /**
      * Object which serves as a surrogate interface, encapsulating a Guacamole
      * client while it is active, allowing it to be maintained in the
@@ -221,6 +231,21 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
          */
         this.clientState = template.clientState || new ManagedClientState();
 
+        /**
+         * Whether a confirmed period of tunnel instability has occurred and
+         * reconnecting this client should be offered if control remains slow.
+         *
+         * @type Boolean
+         */
+        this.reconnectSuggested = template.reconnectSuggested || false;
+
+        /**
+         * Whether an automatic reconnect is currently queued for this client.
+         *
+         * @type Boolean
+         */
+        this.autoReconnectPending = false;
+
         /**
          * Properties associated with the display and behavior of the Guacamole
          * client.
@@ -390,9 +415,91 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
             tunnel : tunnel
         });
 
+        // Downstream modification: avoid showing transient instability caused
+        // by browser background throttling or short network stalls.
+        var unstableWarningTimer = null;
+
+        // ChainedTunnel forwards the active child's state without updating its
+        // own state property, so retain the latest forwarded value explicitly.
+        var currentTunnelState = Guacamole.Tunnel.State.CLOSED;
+
+        /**
+         * Cancels any pending warning that the tunnel appears unstable.
+         */
+        var cancelUnstableWarningTimer = function cancelUnstableWarningTimer() {
+            if (unstableWarningTimer !== null) {
+                $window.clearTimeout(unstableWarningTimer);
+                unstableWarningTimer = null;
+            }
+        };
+
+        /**
+         * Shows the tunnel instability warning only if the tunnel remains
+         * unstable and the page remains visible for the full grace period.
+         */
+        var scheduleUnstableWarning = function scheduleUnstableWarning() {
+
+            cancelUnstableWarningTimer();
+            ManagedClientState.setTunnelUnstable(managedClient.clientState, false);
+
+            // A background page cannot usefully display this warning. If the
+            // tunnel is still unstable on return, visibilitychange will start
+            // a fresh grace period.
+            if ($document[0].hidden)
+                return;
+
+            unstableWarningTimer = $window.setTimeout(function showUnstableWarning() {
+                unstableWarningTimer = null;
+
+                // Tunnel state and page visibility may have changed while the
+                // timer was pending. Recheck both within Angular's digest.
+                $rootScope.$evalAsync(function updateUnstableWarning() {
+                    if (currentTunnelState === Guacamole.Tunnel.State.UNSTABLE
+                            && !$document[0].hidden) {
+                        ManagedClientState.setTunnelUnstable(
+                            managedClient.clientState, true);
+                        managedClient.reconnectSuggested = true;
+                    }
+                });
+
+            }, TUNNEL_UNSTABLE_WARNING_DELAY);
+
+        };
+
+        /**
+         * Restarts the warning grace period when an unstable connection
+         * returns to a visible page, and suppresses the warning while hidden.
+         */
+        var tunnelVisibilityChanged = function tunnelVisibilityChanged() {
+            if (currentTunnelState !== Guacamole.Tunnel.State.UNSTABLE)
+                return;
+
+            $rootScope.$evalAsync(function updateUnstableWarningForVisibility() {
+                scheduleUnstableWarning();
+            });
+        };
+
+        /**
+         * Begins monitoring a persistently unstable tunnel.
+         */
+        var beginUnstableWarning = function beginUnstableWarning() {
+            $document[0].addEventListener('visibilitychange', tunnelVisibilityChanged, false);
+            scheduleUnstableWarning();
+        };
+
+        /**
+         * Clears all warning state once the tunnel changes state.
+         */
+        var endUnstableWarning = function endUnstableWarning() {
+            cancelUnstableWarningTimer();
+            $document[0].removeEventListener('visibilitychange', tunnelVisibilityChanged, false);
+            ManagedClientState.setTunnelUnstable(managedClient.clientState, false);
+        };
+
         // Fire events for tunnel errors
         tunnel.onerror = function tunnelError(status) {
             $rootScope.$apply(function handleTunnelError() {
+                endUnstableWarning();
                 ManagedClientState.setConnectionState(managedClient.clientState,
                     ManagedClientState.ConnectionState.TUNNEL_ERROR,
                     status.code);
@@ -410,28 +517,31 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
 
         // Update connection state as tunnel state changes
         tunnel.onstatechange = function tunnelStateChanged(state) {
+            currentTunnelState = state;
             $rootScope.$evalAsync(function updateTunnelState() {
                 
                 switch (state) {
 
                     // Connection is being established
                     case Guacamole.Tunnel.State.CONNECTING:
+                        endUnstableWarning();
                         ManagedClientState.setConnectionState(managedClient.clientState,
                             ManagedClientState.ConnectionState.CONNECTING);
                         break;
 
                     // Connection is established / no longer unstable
                     case Guacamole.Tunnel.State.OPEN:
-                        ManagedClientState.setTunnelUnstable(managedClient.clientState, false);
+                        endUnstableWarning();
                         break;
 
                     // Connection is established but misbehaving
                     case Guacamole.Tunnel.State.UNSTABLE:
-                        ManagedClientState.setTunnelUnstable(managedClient.clientState, true);
+                        beginUnstableWarning();
                         break;
 
                     // Connection has closed
                     case Guacamole.Tunnel.State.CLOSED:
+                        endUnstableWarning();
                         ManagedClientState.setConnectionState(managedClient.clientState,
                             ManagedClientState.ConnectionState.DISCONNECTED);
                         break;
@@ -958,6 +1068,24 @@ angular.module('client').factory('ManagedClient', ['$rootScope', '$injector',
         return !!(client && client.uploads && client.uploads.length);
     };
 
+    /**
+     * Returns whether the given client has a file transfer which has not yet
+     * completed or failed.
+     *
+     * @param {GuacamoleClient} client
+     *     The client for which active file transfers should be checked.
+     *
+     * @returns {boolean}
+     *     true if at least one file transfer is pending or open.
+     */
+    ManagedClient.hasActiveTransfers = function hasActiveTransfers(client) {
+        return !!(client && client.uploads && _.find(client.uploads, upload => {
+            const state = upload.transferState.streamState;
+            return state === ManagedFileTransferState.StreamState.IDLE
+                || state === ManagedFileTransferState.StreamState.OPEN;
+        }));
+    };
+
     /**
      * Store the thumbnail of the given managed client within the connection
      * history under its associated ID. If the client is not connected, this
diff --git a/guacamole/src/main/frontend/src/app/index/controllers/indexController.js b/guacamole/src/main/frontend/src/app/index/controllers/indexController.js
--- a/guacamole/src/main/frontend/src/app/index/controllers/indexController.js
+++ b/guacamole/src/main/frontend/src/app/index/controllers/indexController.js
@@ -177,11 +177,17 @@ angular.module('index').controller('indexController', ['$scope', '$injector',
 
     // Add default destination for input events
     var sink = new Guacamole.InputSink();
-    $document[0].body.appendChild(sink.getElement());
+    var sinkElement = sink.getElement();
+
+    // Downstream modification: raw-keyboard mode must bypass local IMEs so
+    // the remote desktop's current input language remains authoritative.
+    sinkElement.readOnly = true;
+    sinkElement.setAttribute('inputmode', 'none');
+    $document[0].body.appendChild(sinkElement);
 
     // Create event listeners at the global level
     var keyboard = new Guacamole.Keyboard($document[0]);
-    keyboard.listenTo(sink.getElement());
+    keyboard.listenTo(sinkElement);
 
     // Broadcast keydown events
     keyboard.onkeydown = function onkeydown(keysym) {
@@ -221,6 +227,8 @@ angular.module('index').controller('indexController', ['$scope', '$injector',
 
     // Release all keys when window loses focus
     $window.onblur = function () {
+        keyboardRestorePendingUserGesture = true;
+        keyboardMenuShortcutActive = false;
         keyboard.reset();
     };
 
@@ -250,6 +258,342 @@ angular.module('index').controller('indexController', ['$scope', '$injector',
 
     };
 
+    // Downstream modification: restore global keyboard input after tab changes.
+
+    /**
+     * Keysyms of modifier keys which may remain pressed on the remote desktop
+     * if the browser backgrounds the page before delivering their keyup events.
+     *
+     * @type {!number[]}
+     */
+    var REMOTE_MODIFIER_KEYSYMS = [
+        0xFE03, /* AltGr / ISO level 3 shift */
+        0xFFE1, /* Left shift */
+        0xFFE2, /* Right shift */
+        0xFFE3, /* Left ctrl */
+        0xFFE4, /* Right ctrl */
+        0xFFE7, /* Left meta */
+        0xFFE8, /* Right meta */
+        0xFFE9, /* Left alt */
+        0xFFEA, /* Right alt */
+        0xFFEB, /* Left super / Windows */
+        0xFFEC, /* Right super / Windows */
+        0xFFED, /* Left hyper */
+        0xFFEE  /* Right hyper */
+    ];
+
+    /**
+     * Timeout ID of a queued keyboard restoration, or null if none is queued.
+     * focus and visibilitychange often fire together and should be coalesced.
+     *
+     * @type {?number}
+     */
+    var keyboardRestoreTimer = null;
+
+    /**
+     * Whether a background/freeze event requires one final synchronous restore
+     * from the next trusted user gesture.
+     *
+     * @type {boolean}
+     */
+    var keyboardRestorePendingUserGesture = false;
+
+    /**
+     * Whether the native Guacamole menu shortcut is already being handled.
+     * This prevents repeated keydown events from reopening the menu.
+     *
+     * @type {boolean}
+     */
+    var keyboardMenuShortcutActive = false;
+
+    /**
+     * Cancels any queued asynchronous keyboard restoration.
+     */
+    var cancelKeyboardRestoreTimer = function cancelKeyboardRestoreTimer() {
+        if (keyboardRestoreTimer !== null) {
+            $window.clearTimeout(keyboardRestoreTimer);
+            keyboardRestoreTimer = null;
+        }
+    };
+
+    /**
+     * Returns whether the given element is within a visible local form field.
+     * In that case the hidden Guacamole input sink must not steal focus.
+     *
+     * @param {Element} element
+     *     The element to test.
+     *
+     * @returns {boolean}
+     */
+    var isVisibleLocalInput = function isVisibleLocalInput(element) {
+
+        var textInputTarget = $document[0].querySelector('.text-input .target');
+        while (element && element !== $document[0].body) {
+
+            if (element === sink.getElement() || element === textInputTarget)
+                return false;
+
+            var tagName = element.tagName ? element.tagName.toLowerCase() : '';
+            var isLocalInput = tagName === 'input'
+                    || tagName === 'textarea'
+                    || tagName === 'select'
+                    || tagName === 'button'
+                    || tagName === 'a'
+                    || element.isContentEditable;
+
+            if (isLocalInput) {
+                var rect = element.getBoundingClientRect();
+                return rect.width > 0 && rect.height > 0
+                        && rect.right > 0 && rect.bottom > 0;
+            }
+
+            element = element.parentNode;
+
+        }
+
+        return false;
+
+    };
+
+    /**
+     * Returns whether focus intentionally belongs to a visible local form field.
+     *
+     * @returns {boolean}
+     */
+    var hasVisibleLocalInputFocus = function hasVisibleLocalInputFocus() {
+        return isVisibleLocalInput($document[0].activeElement);
+    };
+
+    /**
+     * Returns whether the given event target belongs to the remote desktop
+     * input surface, including either Guacamole input target.
+     *
+     * @param {Element} element
+     *     The element to test.
+     *
+     * @returns {boolean}
+     */
+    var isRemoteKeyboardTarget = function isRemoteKeyboardTarget(element) {
+
+        var textInputTarget = $document[0].querySelector('.text-input .target');
+        if (element === sink.getElement() || element === textInputTarget
+                || element === $document[0].body
+                || element === $document[0].documentElement)
+            return true;
+
+        return !!(element && element.closest && element.closest('.client-body'));
+
+    };
+
+    /**
+     * Immediately restores keyboard forwarding. Synchronous restoration is used
+     * only for the first trusted pointer gesture after a background/freeze.
+     *
+     * @param {boolean} userInitiated
+     *     Whether this invocation is running within a trusted user gesture.
+     *
+     * @param {boolean} ignoreExistingLocalFocus
+     *     Whether focus held by a previous local form control should be ignored.
+     *
+     * @returns {boolean}
+     *     true if restoration was performed, false otherwise.
+     */
+    var restoreRemoteKeyboardInputNow = function restoreRemoteKeyboardInputNow(
+            userInitiated, ignoreExistingLocalFocus) {
+
+        if ($document[0].hidden || !$document[0].hasFocus()
+                || $scope.applicationState !== ApplicationState.READY
+                || !hasActiveTunnel()
+                || (!ignoreExistingLocalFocus && hasVisibleLocalInputFocus()))
+            return false;
+
+        $scope.$broadcast('guacKeyboardFocusRequested');
+
+        keyboard.reset();
+        angular.forEach(REMOTE_MODIFIER_KEYSYMS, function releaseModifier(keysym) {
+            $scope.$broadcast('guacSyntheticKeyup', keysym);
+        });
+
+        var inputFocusEvent = $scope.$broadcast(
+                'guacInputFocusRestoreRequested', userInitiated);
+        if (!inputFocusEvent.defaultPrevented) {
+            var sinkElement = sink.getElement();
+            sinkElement.value = '';
+            sinkElement.blur();
+            if (userInitiated)
+                sinkElement.focus();
+            else
+                sink.focus();
+        }
+
+        return true;
+
+    };
+
+    /**
+     * Restores raw keyboard forwarding after returning to the browser tab. This
+     * is the path used when Guacamole input mode is "None" and Microsoft Pinyin
+     * or another IME runs inside the remote Windows session.
+     */
+    var restoreRemoteKeyboardInput = function restoreRemoteKeyboardInput() {
+
+        if ($document[0].hidden || !$document[0].hasFocus()
+                || $scope.applicationState !== ApplicationState.READY
+                || !hasActiveTunnel() || hasVisibleLocalInputFocus()
+                || keyboardRestoreTimer !== null)
+            return;
+
+        keyboardRestoreTimer = $window.setTimeout(function deferKeyboardRestore() {
+
+            keyboardRestoreTimer = null;
+
+            if ($document[0].hidden || !$document[0].hasFocus()
+                    || $scope.applicationState !== ApplicationState.READY
+                    || !hasActiveTunnel() || hasVisibleLocalInputFocus())
+                return;
+
+            $scope.$evalAsync(function restoreKeyboardWithinDigest() {
+                restoreRemoteKeyboardInputNow(false, false);
+            });
+
+        }, 0);
+
+    };
+
+    // Some browsers do not reliably fire window blur for tab switches. Always
+    // reset tracked keys on visibility loss and restore the input sink on return.
+    var keyboardVisibilityChanged = function keyboardVisibilityChanged() {
+
+        if ($document[0].hidden) {
+            keyboardRestorePendingUserGesture = true;
+            keyboardMenuShortcutActive = false;
+            cancelKeyboardRestoreTimer();
+            keyboard.reset();
+        }
+        else
+            restoreRemoteKeyboardInput();
+
+    };
+
+    /**
+     * Marks keyboard input as needing a trusted-gesture restore after the page
+     * lifecycle freezes or resumes.
+     */
+    var keyboardPageLifecycleChanged = function keyboardPageLifecycleChanged(e) {
+
+        keyboardRestorePendingUserGesture = true;
+        keyboardMenuShortcutActive = false;
+
+        if (e.type === 'freeze' || e.type === 'pagehide') {
+            cancelKeyboardRestoreTimer();
+            keyboard.reset();
+        }
+        else
+            restoreRemoteKeyboardInput();
+
+    };
+
+    /**
+     * Performs the final restoration synchronously on a trusted mouse, touch,
+     * or pointer gesture over the remote desktop after the page has returned
+     * from the background. Multiple event types are intentionally supported
+     * because the earliest event may run before the browser has restored focus.
+     */
+    var keyboardUserGesture = function keyboardUserGesture(e) {
+
+        if (!keyboardRestorePendingUserGesture || $document[0].hidden
+                || e.isTrusted === false || !isRemoteKeyboardTarget(e.target)
+                || isVisibleLocalInput(e.target))
+            return;
+
+        cancelKeyboardRestoreTimer();
+
+        // pointerdown/mousedown/touchstart run before the browser's default
+        // focus action, which may immediately steal focus again. Keep recovery
+        // armed until the bubbling click performs the final restoration.
+        if (restoreRemoteKeyboardInputNow(true, true) && e.type === 'click')
+            keyboardRestorePendingUserGesture = false;
+
+    };
+
+    /**
+     * Restores keyboard capture before Guacamole handles the first key after a
+     * background event. Ctrl-Alt-K always forces recovery, while the native
+     * Ctrl-Alt-Shift shortcut also recovers input before directly opening the
+     * Guacamole menu. Both shortcuts work even if lifecycle events were omitted.
+     */
+    var keyboardKeyGesture = function keyboardKeyGesture(e) {
+
+        var altGraph = e.getModifierState
+                && e.getModifierState('AltGraph');
+        var forceRecovery = e.ctrlKey && e.altKey && !e.shiftKey
+                && !altGraph
+                && (e.code === 'KeyK' || e.key === 'K' || e.key === 'k');
+        var forceMenu = e.ctrlKey && e.altKey && e.shiftKey && !altGraph;
+
+        if (forceRecovery || forceMenu) {
+            e.preventDefault();
+            e.stopImmediatePropagation();
+
+            if (e.repeat || (forceMenu && keyboardMenuShortcutActive))
+                return;
+
+            keyboardMenuShortcutActive = forceMenu;
+            keyboardRestorePendingUserGesture = true;
+        }
+
+        if (!keyboardRestorePendingUserGesture || $document[0].hidden
+                || e.isTrusted === false
+                || (!forceRecovery && !forceMenu
+                        && (!isRemoteKeyboardTarget(e.target)
+                        || isVisibleLocalInput(e.target))))
+            return;
+
+        cancelKeyboardRestoreTimer();
+
+        if (restoreRemoteKeyboardInputNow(true, true))
+            keyboardRestorePendingUserGesture = false;
+
+        if (forceMenu)
+            $scope.$evalAsync(function toggleGuacamoleMenu() {
+                $scope.$broadcast('guacToggleMenuRequested');
+            });
+
+    };
+
+    /**
+     * Allows the native Guacamole menu shortcut to be used again after any of
+     * its modifier keys has been released.
+     */
+    var keyboardKeyReleased = function keyboardKeyReleased(e) {
+        if (!(e.ctrlKey && e.altKey && e.shiftKey))
+            keyboardMenuShortcutActive = false;
+    };
+
+    /**
+     * Forces full keyboard/input recovery from a Guacamole menu action.
+     */
+    $scope.$on('guacForceInputRecovery', function forceInputRecovery() {
+        keyboardRestorePendingUserGesture = true;
+        cancelKeyboardRestoreTimer();
+        if (restoreRemoteKeyboardInputNow(true, true))
+            keyboardRestorePendingUserGesture = false;
+    });
+
+    $document[0].addEventListener('visibilitychange', keyboardVisibilityChanged, true);
+    $document[0].addEventListener('freeze', keyboardPageLifecycleChanged, true);
+    $document[0].addEventListener('resume', keyboardPageLifecycleChanged, true);
+    $document[0].addEventListener('fullscreenchange', keyboardPageLifecycleChanged, true);
+    $document[0].addEventListener('pointerlockchange', keyboardPageLifecycleChanged, true);
+    $document[0].addEventListener('pointerdown', keyboardUserGesture, true);
+    $document[0].addEventListener('mousedown', keyboardUserGesture, true);
+    $document[0].addEventListener('touchstart', keyboardUserGesture, true);
+    $document[0].addEventListener('click', keyboardUserGesture, false);
+    $window.addEventListener('keydown', keyboardKeyGesture, true);
+    $window.addEventListener('keyup', keyboardKeyReleased, true);
+    $window.addEventListener('pagehide', keyboardPageLifecycleChanged, true);
+    $window.addEventListener('pageshow', keyboardPageLifecycleChanged, true);
+
     // If we're logged in and not connected to anything, periodically check
     // whether the current session is still valid. If the session has expired,
     // refresh the auth state to reshow the login screen (rather than wait for
@@ -278,9 +622,12 @@ angular.module('index').controller('indexController', ['$scope', '$injector',
     $window.addEventListener('cut',   clipboardService.resyncClipboard);
     $window.addEventListener('focus', function focusGained(e) {
 
-        // Only recheck clipboard if it's the window itself that gained focus
-        if (e.target === $window)
-            clipboardService.resyncClipboard();
+        // Only act if it is the window itself that gained focus.
+        if (e.target !== $window)
+            return;
+
+        clipboardService.resyncClipboard();
+        restoreRemoteKeyboardInput();
 
     }, true);
 
diff --git a/guacamole/src/main/frontend/src/app/textInput/directives/guacTextInput.js b/guacamole/src/main/frontend/src/app/textInput/directives/guacTextInput.js
--- a/guacamole/src/main/frontend/src/app/textInput/directives/guacTextInput.js
+++ b/guacamole/src/main/frontend/src/app/textInput/directives/guacTextInput.js
@@ -28,8 +28,10 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
         scope: {},
 
         templateUrl: 'app/textInput/templates/guacTextInput.html',
-        controller: ['$scope', '$rootScope', '$element', '$timeout',
-            function guacTextInput($scope, $rootScope, $element, $timeout) {
+        controller: ['$scope', '$rootScope', '$element', '$timeout', '$window',
+            function guacTextInput($scope, $rootScope, $element, $timeout, $window) {
+
+            // Downstream modification: recover IME input after browser tab changes.
 
             /**
              * The number of characters to include on either side of text input
@@ -118,6 +120,13 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
              */
             var target = $element.find('.target')[0];
 
+            /**
+             * The browser document containing the text input target.
+             *
+             * @type Document
+             */
+            var document = $window.document;
+
             /**
              * Whether the text input target currently has focus. Setting this
              * attribute has no effect, but any bound property will be updated
@@ -129,7 +138,9 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
 
             target.onfocus = function targetFocusGained() {
                 hasFocus = true;
+                composingText = false;
                 resetTextInputTarget(TEXT_INPUT_PADDING);
+                $rootScope.$broadcast('guacTextInputFocusRequested');
             };
 
             target.onblur = function targetFocusLost() {
@@ -149,7 +160,20 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
             }, false);
 
             target.addEventListener("compositionend", function targetComposeEnd(e) {
+
                 composingText = false;
+
+                // Some browsers deliver the final input event before the
+                // compositionend event. In that case the input event is ignored
+                // while composingText is true. Process any remaining committed
+                // text once composition has ended.
+                $window.setTimeout(function flushCompletedComposition() {
+                    var paddingChar = String.fromCharCode(TEXT_INPUT_PADDING_CODEPOINT);
+                    var emptyValue = new Array(TEXT_INPUT_PADDING * 2 + 1).join(paddingChar);
+                    if (!composingText && target.value !== emptyValue)
+                        processTextInput();
+                }, 0);
+
             }, false);
 
             /**
@@ -281,7 +305,14 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
 
             };
 
-            target.addEventListener("input", function(e) {
+            /**
+             * Processes the current contents of the text input target, sending
+             * the corresponding text, Backspace, or Delete events remotely.
+             *
+             * @param {InputEvent} [e]
+             *     The browser input event that triggered processing, if any.
+             */
+            var processTextInput = function processTextInput(e) {
 
                 // Ignore input events during text composition
                 if (composingText)
@@ -311,9 +342,120 @@ angular.module('textInput').directive('guacTextInput', [function guacTextInput()
 
                 // Reset content
                 resetTextInputTarget(TEXT_INPUT_PADDING);
-                e.preventDefault();
+                if (e)
+                    e.preventDefault();
+
+            };
+
+            target.addEventListener("input", processTextInput, false);
+
+            /**
+             * Clears composition state after the browser page loses or regains
+             * visibility/focus. A browser may omit compositionend while a tab
+             * is backgrounded, otherwise leaving composingText permanently true.
+             */
+            var resetAfterBrowserFocusChange = function resetAfterBrowserFocusChange() {
+
+                composingText = false;
+                resetTextInputTarget(TEXT_INPUT_PADDING);
+
+                // Ensure synthetic text, Backspace, and Delete events are routed
+                // to an active Guacamole client after returning to this tab.
+                if (!document.hidden)
+                    $rootScope.$broadcast('guacTextInputFocusRequested');
+
+            };
+
+            document.addEventListener("visibilitychange", resetAfterBrowserFocusChange, false);
+            $window.addEventListener("focus", resetAfterBrowserFocusChange, false);
+
+            /**
+             * Returns whether focus intentionally belongs to a visible local
+             * form control instead of the remote text-input target.
+             *
+             * @param {Element} element
+             *     The element to test.
+             *
+             * @returns {boolean}
+             */
+            var isVisibleLocalInput = function isVisibleLocalInput(element) {
+
+                while (element && element !== document.body) {
+
+                    if (element === target)
+                        return false;
+
+                    var tagName = element.tagName
+                            ? element.tagName.toLowerCase() : '';
+                    var isLocalInput = tagName === 'input'
+                            || tagName === 'textarea'
+                            || tagName === 'select'
+                            || tagName === 'button'
+                            || tagName === 'a'
+                            || element.isContentEditable;
+
+                    if (isLocalInput) {
+                        var rect = element.getBoundingClientRect();
+                        return rect.width > 0 && rect.height > 0
+                                && rect.right > 0 && rect.bottom > 0;
+                    }
+
+                    element = element.parentNode;
+
+                }
+
+                return false;
+
+            };
+
+            /**
+             * Restores focus to the visible text-input target without stealing
+             * focus from a local Guacamole form control.
+             */
+            var focusTextInputTarget = function focusTextInputTarget() {
+
+                if (document.hidden)
+                    return;
+
+                var focused = document.activeElement;
+                if (!isVisibleLocalInput(focused)) {
+
+                    // Recreate the native editing context if Chromium kept this
+                    // textarea active while suspending its IME session.
+                    if (focused === target)
+                        target.blur();
+                    target.focus();
+
+                }
+
+            };
+
+            // If global keyboard restoration occurs while text-input mode is
+            // active, keep focus on this visible textarea instead of allowing
+            // the hidden raw-keyboard InputSink to steal it.
+            $scope.$on('guacInputFocusRestoreRequested', function restoreTextInputFocus(event, immediate) {
+
+                event.preventDefault();
+                composingText = false;
+                resetTextInputTarget(TEXT_INPUT_PADDING);
+                $rootScope.$broadcast('guacTextInputFocusRequested');
+
+                // A pointerdown-triggered restoration must remain synchronous so
+                // Chromium associates the new editing context with that trusted
+                // user gesture. Other restoration events remain deferred.
+                if (immediate)
+                    focusTextInputTarget();
+                else
+                    $window.setTimeout(focusTextInputTarget, 0);
+
+            });
+
+            // Prevent stale handlers after navigating away from the client view.
+            $scope.$on('$destroy', function textInputDestroyed() {
+                document.removeEventListener("visibilitychange", resetAfterBrowserFocusChange, false);
+                $window.removeEventListener("focus", resetAfterBrowserFocusChange, false);
+            });
 
-            }, false);
 
             // Do not allow event target contents to be selected during input
             target.addEventListener("selectstart", function(e) {
diff --git a/guacamole/src/main/frontend/src/translations/en.json b/guacamole/src/main/frontend/src/translations/en.json
--- a/guacamole/src/main/frontend/src/translations/en.json
+++ b/guacamole/src/main/frontend/src/translations/en.json
@@ -74,6 +74,7 @@
         "ACTION_NAVIGATE_BACK"             : "@:APP.ACTION_NAVIGATE_BACK",
         "ACTION_NAVIGATE_HOME"             : "@:APP.ACTION_NAVIGATE_HOME",
         "ACTION_RECONNECT"                 : "Reconnect",
+        "ACTION_DISMISS_RECOVERY"          : "Keep current session",
         "ACTION_SAVE_FILE"                 : "@:APP.ACTION_SAVE",
         "ACTION_SHARE"                     : "@:APP.ACTION_SHARE",
         "ACTION_SHOW_CLIPBOARD"            : "Click to view clipboard contents.",
@@ -124,10 +125,13 @@
 
         "FIELD_PLACEHOLDER_FILTER" : "@:APP.FIELD_PLACEHOLDER_FILTER",
 
+        "ACTION_RECOVER_KEYBOARD" : "Recapture keyboard",
+
         "HELP_CLIPBOARD"           : "Text copied/cut within Guacamole will appear here. Changes to the text below will affect the remote clipboard.",
         "HELP_INPUT_METHOD_NONE"   : "No input method is used. Keyboard input is accepted from a connected, physical keyboard.",
         "HELP_INPUT_METHOD_OSK"    : "Display and accept input from the built-in Guacamole on-screen keyboard. The on-screen keyboard allows typing of key combinations that may otherwise be impossible (such as Ctrl-Alt-Del).",
         "HELP_INPUT_METHOD_TEXT"   : "Allow typing of text, and emulate keyboard events based on the typed text. This is necessary for devices such as mobile phones that lack a physical keyboard.",
+        "HELP_RECOVER_KEYBOARD"    : "Reset and recapture keyboard and IME input without refreshing. You can also press Ctrl-Alt-K.",
         "HELP_MOUSE_MODE"          : "Determines how the remote mouse behaves with respect to touches.",
         "HELP_MOUSE_MODE_ABSOLUTE" : "Tap to click. The click occurs at the location of the touch.",
         "HELP_MOUSE_MODE_RELATIVE" : "Drag to move the mouse pointer and tap to click. The click occurs at the location of the pointer.",
@@ -164,6 +168,8 @@
         "TEXT_CLIENT_STATUS_CONNECTING"   : "Connecting to Guacamole...",
         "TEXT_CLIENT_STATUS_DISCONNECTED" : "You have been disconnected.",
         "TEXT_CLIENT_STATUS_UNSTABLE"     : "The network connection to the Guacamole server appears unstable.",
+        "TEXT_CLIENT_STATUS_RECOVERED_SLOW" : "The network connection recovered. If the display or controls remain slow, reconnect this session.",
+        "TEXT_CLIENT_STATUS_RECOVERED_RECONNECTING" : "The network connection recovered. This session will reconnect automatically to clear residual delay.",
         "TEXT_CLIENT_STATUS_WAITING"      : "Connected to Guacamole. Waiting for response...",
         "TEXT_USER_JOINED"                : "{USERNAME} has joined the connection.",
         "TEXT_USER_LEFT"                  : "{USERNAME} has left the connection.",
diff --git a/guacamole/src/main/frontend/src/translations/zh.json b/guacamole/src/main/frontend/src/translations/zh.json
--- a/guacamole/src/main/frontend/src/translations/zh.json
+++ b/guacamole/src/main/frontend/src/translations/zh.json
@@ -70,6 +70,7 @@
         "ACTION_NAVIGATE_BACK"             : "@:APP.ACTION_NAVIGATE_BACK",
         "ACTION_NAVIGATE_HOME"             : "@:APP.ACTION_NAVIGATE_HOME",
         "ACTION_RECONNECT"                 : "重新连接",
+        "ACTION_DISMISS_RECOVERY"          : "保留当前连接",
         "ACTION_SAVE_FILE"                 : "@:APP.ACTION_SAVE",
         "ACTION_SHARE"                     : "@:APP.ACTION_SHARE",
         "ACTION_SHOW_CLIPBOARD"            : "点击以查看剪贴板内容。",
@@ -120,10 +121,13 @@
 
         "FIELD_PLACEHOLDER_FILTER" : "@:APP.FIELD_PLACEHOLDER_FILTER",
 
+        "ACTION_RECOVER_KEYBOARD" : "重新捕获键盘",
+
         "HELP_CLIPBOARD"           : "复制/剪切的文本将出现在这里。对下面文本内容所作的修改将会影响远程电脑上的剪贴板。",
         "HELP_INPUT_METHOD_NONE"   : "没有选择任何输入方式。将从连接的物理键盘接受键盘输入。",
         "HELP_INPUT_METHOD_OSK"    : "显示并从内建的 Guacamole 屏幕键盘接受输入。屏幕键盘可以输入平常无法输入的按键组合（如 Ctrl-Alt-Del 等）。",
         "HELP_INPUT_METHOD_TEXT"   : "允许输入文本，并根据所输入的文本模拟键盘事件。可用于没有物理键盘的设备，如手机等。",
+        "HELP_RECOVER_KEYBOARD"    : "无需刷新即可重置并重新捕获键盘和输入法。也可按 Ctrl-Alt-K。",
         "HELP_MOUSE_MODE"          : "设置远程电脑上的鼠标对触控行为的反应。",
         "HELP_MOUSE_MODE_ABSOLUTE" : "点击时立即触发按键。在点击的位置触发鼠标按键事件。",
         "HELP_MOUSE_MODE_RELATIVE" : "拖拽时移动鼠标，再点击时触发按键。在鼠标当前所在的位置触发按键事件。",
@@ -160,6 +164,8 @@
         "TEXT_CLIENT_STATUS_CONNECTING"   : "正在连接 Guacamole...",
         "TEXT_CLIENT_STATUS_DISCONNECTED" : "您的连接已断开。",
         "TEXT_CLIENT_STATUS_UNSTABLE"     : "到 Guacamole 服务器的网络连接似乎不太稳定。",
+        "TEXT_CLIENT_STATUS_RECOVERED_SLOW" : "网络连接已经恢复。如果画面或控制仍然卡顿，请重新连接当前会话。",
+        "TEXT_CLIENT_STATUS_RECOVERED_RECONNECTING" : "网络连接已经恢复。系统将自动重新连接当前会话，以清除残留延迟。",
         "TEXT_CLIENT_STATUS_WAITING"      : "已连接到 Guacamole。正在等待应答...",
         "TEXT_USER_JOINED"                : "用户 {USERNAME} 已加入连接。",
         "TEXT_USER_LEFT"                  : "用户 {USERNAME} 断开了连接。",
